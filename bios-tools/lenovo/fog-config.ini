;
; Lenovo Think BIOS Configuration for FOG Imaging
; Format: SettingName,Value
;
; Compatible with ThinkCentre, ThinkPad, ThinkStation models
; Use ThinkBiosConfig.hta to apply: mshta ThinkBiosConfig.hta "file:fog-config.ini"
;

; === Security Settings ===
SecureBoot,Disable

; === Network Boot Settings ===
; ThinkPad settings
EthernetLANOptionROM,Enable
IPv4NetworkStack,Enable
IPv6NetworkStack,Disable
WakeOnLAN,Enable
WakeOnLANfromDock,Enable

; ThinkCentre/ThinkStation settings  
NetworkBoot,Enable
PXEIPv4NetworkStack,Enable

; === Boot Settings ===
; Note: Boot order may need to be set manually or use BootOrder setting
; BootOrder format varies by model

; === Power Settings ===
AfterPowerLoss,Power On
WakeOnLAN,Enable

; === Performance/Other ===
FastBoot,Disable
